name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./chat-app

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
<<<<<<< HEAD
          branch: gh-pages
          folder: ./chat-app/dist
          clean: true
=======
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./chat-app/dist
>>>>>>> 103103ac24685dae2cda90b3849f30fe455eb4ed
